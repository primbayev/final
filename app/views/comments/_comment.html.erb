<li>
  <p>
    On <%= comment.created_at.to_date %>, <strong><%= comment.user.name %></strong> said:
  </p>
  <p>
    <%= comment.body %>
  </p>

  <p>
    Quality of food: <strong> <%= comment.food_quality %> </strong><br>

    Service quality: <strong> <%= comment.service_quality %></strong> <br>

    Interior: <strong> <%= comment.interior %></strong>
  </p>

  <% if current_user == comment.user %>
    <div>
      <%= link_to 'Delete', place_comment_path(comment.place, comment),
        {
          method: :delete,
          data: {confirm: 'Are you sure' },
          class: 'btn btn-danger'
        }
      %>
    </div>
  <% end %>

</li>
